import { FaInfoCircle } from "react-icons/fa";

export const Info = () => 
<div className="alert alert-info">
  <FaInfoCircle /> Type in an expression to select the values in the <code>select</code>s
  or pick the values in the <code>select</code>s to get an expression.
</div>